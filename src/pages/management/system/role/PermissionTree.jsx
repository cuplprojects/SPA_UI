// import React from 'react';
// import { Tree } from 'antd';

// const PermissionTree = ({ permissions = [] }) => {
//   const renderTreeNodes = (data) =>
//     data.map((item) => ({
//       ...item,
//       title: item.name,
//       key: item.id,
//       children: item.children && renderTreeNodes(item.children),
//     }));

//   return (
//     <Tree
//       checkable
//       treeData={renderTreeNodes(permissions)}
//       defaultExpandAll
//       treeCheckStrictly
//     />
//   );
// };

// export default PermissionTree;
